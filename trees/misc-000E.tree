\date{2024-12-14}
\title{dissolution locale}
\author{trebor}
\import{common}
\taxon{definition}

\p{
  Given a locale #{X}, if we have a map #{f : Y \to X} such that the preimage of every sublocale is closed, and every map #{g : Z \to X} satisfying the same property factors through #{f} uniquely, then we say #{Y} is the \strong{dissolution} of #{X}.
}
