\date{2024-02-21}
\title{chain reduction}
\taxon{definition}
\author{trebor}
\import{common}

\p{
  Given two chain complexes #{C, D}, a \strong{chain reduction} consists of a chain map #{f : C \to D}, a chain map #{g : D \to C} and a [chain homotopy](hmlg-0016) #{h : \id \to g \circ f}, such that #{f \circ g = \id}, and ##{f \circ h = h \circ g = h \circ h = 0.} Here the compositions are understood to have the appropriate subscript so the degrees match. We write the reduction as #{C \Reduce D}.
}
